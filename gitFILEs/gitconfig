[user]
	name = Masafumi Horimoto on sopi2
        email = horimoto@holly-linux.com
[core]
        editor = vi -c \"set fenc=utf-8\"
        quotepath = false
[push]
        default = matching
[color]
        diff = auto
        status = auto
        branch = auto
[alias]
        hist = log --graph --date=short
        tree = log --graph --all --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(bold white)— %an%C(reset)%C(bold yellow)%d%C(reset)' --abbrev-commit --date=relative
[pager]
        log = less

